* fix generatePatchedAppWasm.py so the imports are grabbed from index.html instead of adding new ones... it seems like i cant add custom imports and have to depend on the ones given in the index.html for now...

*try to add injected code to the beginning of one function, then add it to all of them.
then we make that debug inject code which calls an imported debug function from js.

*try breaking on all the received packets and print out all the bigswitchfunc indexe files up to 500 lines or so, maybe i can pin point the decoding structure or something.

*do the same for length 15 and 13 reciving packets? we dont send anything for those lengths only receive, so i need to find a better place for the breakpoint to stop the stuff...
probably print out the first 1000 or something in the bigSwitchFunc.. gotta figure that one out...

*make a script that generates the import header file?
probably also a mapping between imports that have certain code in them and assingning them a specific name in the header file.