* research the "n[0]" and "fa" locals, see "workspace/experiment_bigfunc1/bigfunc.notes".

* i wanna look into using rust cus it might be easier to use mmaayyybeeeee... idk yet...

* add a special_packetsender_num for that one function that sends packets other than bigfunc.

* log the last and first N indexes that are visited before a bigfunc call returns.

* compiler feature: implement injecting table instructions. also call_indirect.

* source code primitive: implement a global.set special function.

* add some standard library things like: hashmap, linkedlist, arena allocator.

* source code primitive: implement inject_imports? a way to run code before app.wat calls an imported function.

* source code primitive: maybe, add a special_export_a_func_num() so there is a way to get the func nums for the arras exported funcs.

* investigate how 266 is being called like 10 tens in a row with the same two arguments, i saw that on the length 74 packet receive debug calls only trace.

* chacha1_finish is broken? where are sent packets handled... i know chacha2_finish does received packets...
